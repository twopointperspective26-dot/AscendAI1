# Ascend AI - MVP Landing Dashboard

This workspace contains a minimal Next.js + Tailwind CSS app for the Ascend AI landing dashboard.

Quick start:

1. Install dependencies: `npm install` or `pnpm install`
2. Run dev server: `npm run dev`

App entry: `app/page.tsx`
# AscendAI1